**/coc

# Per-project context dumps: nothing but ticket ids, PR links and branch names,
# so there is no version worth publishing. Kept on disk behind the stow symlink.
claude/.claude/commands/smart-analytics-context.md
claude/.claude/commands/translocation-tests-stack.md
